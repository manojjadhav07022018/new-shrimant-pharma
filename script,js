// JavaScript for "Add to Cart" functionality (demo)
document.querySelectorAll('.add-to-cart').forEach(button => {
    button.addEventListener('click', function() {
        alert('Product added to your cart!');
    });
});
