// Example JavaScript to handle Add to Cart functionality (basic example)
document.querySelectorAll('.add-to-cart').forEach(button => {
    button.addEventListener('click', () => {
        alert("Product added to cart!");
    });
});
